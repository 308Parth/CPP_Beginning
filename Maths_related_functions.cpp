#include <iostream>
#include <cmath>

int main()
{
    double x = 3.98;
    double y = 4;
    double z;

    //z = std::max(x, y);
    //z = std::min(x,y);
    //z = pow(2, 3);   //This is how to add power. Here it means 2 to the power 3.
    //z = sqrt(9);     //For square root
    //z = abs(-3);     //Mod
    //z = round(x);    //Approx
    //z = ceil(x);     //Greatest integer*
    //z = floor(x);    //will always return to the number which is infrount of .
    
    std::cout << z;

    return 0;
}